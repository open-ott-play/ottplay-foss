/**
 * MobileNativeMedia plugin — thin re-export so src/index.ts can import
 * from the plugins directory alongside m3u-proxy.
 */
export { MobileNativeMedia } from "../mobile-native-media/src/index";
