# Audit queue

Functions to audit for port parity (legacy → current):

1. `initPlayer`
2. `keyHandler`
3. `setListDataArray`
4. `getListDataArray`
5. `setListSelectionIndex`
6. `getListSelectionIndex`
7. `setGetListItemFn`
8. `setDetailListActionFn`
9. `setListKeyHandlerFn`
10. `searchChannel`
11. `searchMedia`
12. `searchRec`
13. `getFilteredHistory`
14. `getFilteredChannelList`
15. `playChannel`
16. `stopPlayer`
17. `setVolume`
18. `getVolume`
19. `toggleFullscreen`
20. `setChannelList`
21. `getChannelList`

---

## Done:
- `getFilteredHistory` — absent from legacy (`stbPlayer.js`); TS-only feature added for search functionality. Implementation at `src/channels/index.ts:2650` filters `medHistory` by `historySearchText` on `name`/`title` fields. No legacy counterpart to match. No fix needed.
- `searchHistoryChannel` / `getFilteredHistory` — both absent from legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2815` and `:2823`. No legacy counterpart. Both are dead code: `searchHistoryChannel` has no callers outside its own module; `getFilteredHistory` is only consumed by `searchHistoryChannel`. State `historySearchText` (`src/channels/index.ts:219`) is orphaned after removal. Fix: removed both functions and `historySearchText`. Supersedes PR #149 "no fix" decision for `getFilteredHistory`.
- `setListKeyHandlerFn` — absent from legacy (`stbPlayer.js`); legacy uses `var listKeyHandler;` (L1530) + direct global assignment (L1846, L2322, L2748, L2996, L3064, L3132, L3517, L4327, L5032, L5206, L5333, L5711, L5928, L8205, L8290, L8326) + invoke (L7146); TS added setter `setListKeyHandlerFn(val)` (`src/app/state.ts:120`) writing to module-local `listKeyHandlerFn` (`src/app/state.ts:40`) — no other module imports `listKeyHandlerFn` from `state.ts`, setter target is dead. Fix: removed `export let listKeyHandlerFn` and `setListKeyHandlerFn` from `src/app/state.ts`; updated caller `src/view/channel-list.ts:27` to assign `(window as any).listKeyHandler = fn` directly, matching legacy's global-assignment pattern.
- `setGetListItemFn` — missing in legacy (`stbPlayer.js`); present in TS at `src/app/state.ts:112`. No legacy counterpart to match. No fix needed.
- `setEdit` — legacy `setEdit = function()` (`stbPlayer.js:L3863` var decl, `L7997` inside `edit_dealer()`); current `setEdit` assigned in `src/provider/index.ts:1427` inside `edit_dealer()`; fix: remove no-op stub from `src/app/init.ts:179` — TS-only default absent in legacy. Legacy declares `setEdit` uninitialized; no default body exists. All callers (`ui/index.ts:173`, `ui/index.ts:2915`, `keyhandler/index.ts:609`) guard with `typeof setEdit === "function"` before invoking, so the stub is unreachable dead code.
- `closeList` — legacy `closeList()` (`stbPlayer.js:L1723`); current `closeList()` (`src/ui/index.ts:L776`); fix: add missing `$("#listPopUp").hide()`, PiP restore (`!sNoSmall && pipIndex != null → stbPlayPip(...)`), and preview-channel resume (`sPreview && previewChan → stbStop/playArchive/playChannel + previewChan = null`) — three blocks present in legacy but absent from TypeScript port.
- `showEditKey1` — legacy `showEditKey1(e)` (`stbPlayer.js:L3988`); current `showEditKey1(_initKeys)` (`src/ui/index.ts:L2642`); fix: no mismatch — body parity exact (saveCPD, lang check, _keysSymbol[1/7/9], sNoColorKeys underlines, editPos, _keyCur clamp, _setPunct + showEdit).
- `editKey2` — legacy `stbPlayer.js:L7962` referenced only as `editKey = editKey2` in `setEditor()`; never defined as a function in legacy (assumed external global); current `src/ui/index.ts:L2931` `editKey2(code: number): void` with switch on `keys.ENTER`/`keys.EXIT`/`keys.RETURN`; callers: `src/index.ts:933` + `src/view/display-helpers.ts:329` alias `window.editKey = window.editKey2`; status: partial — legacy has no body to match; no TS fix needed.
- `shiftArchive` — legacy `shiftArchive()` (`stbPlayer.js:L6148`) + `_shiftArchive()` (`stbPlayer.js:L6161`); current `shiftArchive()` + `_shiftArchive()` (`src/channels/index.ts:L1805`); fix: when `e === -6e6`, use assignment `_shiftSec = e` (not `_shiftSec += e`) before calling `_shiftArchive()` to match legacy L6150.
- `btnDiv` — legacy `stbPlayer.js:L2651` `btnDiv(e, t, r, s, n)` with guard `if (!r || !e) return ""`, localized description, color class by key, number-key stripping, sNoColorKeys suppression, sNoNumbersKeys stripping; TS `src/ui/index.ts:L1210` matches: signature `btnDiv(keyLabel, label, description, num?, extra?)`, same guard logic, same color/sNumber/sNoColor handling, same HTML output; no discrepancies.
- `shiftArchive` — legacy `shiftArchive()` (`stbPlayer.js:L6148`) + `_shiftArchive()` (`stbPlayer.js:L6161`); current `shiftArchive()` + `_shiftArchive()` (`src/channels/index.ts:L1805`); fix: when `e === -6e6`, use assignment `_shiftSec = e` (not `_shiftSec += e`) before calling `_shiftArchive()` to match legacy L6150.
- `keyFun` — legacy `stbPlayer.js:L7377` `keyFun(e)` with switch cases 0-21 calling global functions directly; current TS `src/keyhandler/index.ts:692` `keyFun(fn: number)` now matches: cases 20/21 have `typeof` guards removed, all other cases retain their guards per legacy/stbPlayer.js implementation; function signature `keyFun(fn: number): void` compatible; all parameter usage and side effects match.
- `showActionsDialog` — legacy `stbPlayer.js:L2502` anonymous dialog-builder `a()` + `dialogBoxKeyHandler`; TS `src/channels/index.ts:L2515` now matches: removed `typeof fn === "function"` guards from all `dialogBoxKeyHandler` cases (ENTER/UP/DOWN/LEFT/RIGHT/RETURN/YELLOW/TOOLS), removed conditional guard on `keys.RIGHT` (`w.sPSchannels && w.parentPIN != "*"`), changed return statements to bare `return;` instead of `return true;`/`return false;`, removed `return false;` after switch; removed `: boolean` return type annotation.
- `restoreCPD` — legacy `stbPlayer.js:L7980` `restoreCPD()` with bare `listCaption.innerHTML = ui_state.lc`, `listPodval.innerHTML = ui_state.lp`, `listDetail.innerHTML = ui_state.ld`, `ui_state = {}`; TS `src/ui/index.ts:L1189` now matches: removed null guards on `listCaptionElement`/`listPodvalElement`/`listDetailElement`, removed `|| ""` fallbacks (`ui_state.lc || ""`), direct assignments `listCaptionElement.innerHTML = ui_state.lc`, `listPodvalElement.innerHTML = ui_state.lp`, `listDetailElement.innerHTML = ui_state.ld`; stripped null-safe guards and optional chaining.
- `timeShift` — legacy `stbPlayer.js:L6302` `timeShift(n)` with bare `getEPGchanelCached`, `chanels[e].rec`, `setCurProg(t, e, null)`, `showShift(step2text(-n))`, `playArchive(Math.round(Date.now() / 1e3) - n)`, `showShift(_("Archive - begin"))`, `epgArray[s].time`; TS `src/channels/index.ts:L1942` now matches: local `getEPGchanelCached` call, `channels[chId].rec` guard, `setCurProg(chId, epgData, null)`, `showShift(step2text(-n))`, inline `playArchive(Math.round(Date.now() / 1000) - n)`, `showShift(_("Archive - begin"))`, `playArchive(r[s].time)`; stripped extra `getEPGchanelCached` typeof guard fallback, `ch!.rec!` non-null assertions, `(window as any).epgArray`/`curProg` writes, `playArchive` s>=0 guard, `showShift` typeof guards, `step2text`/_() typeof fallbacks.
- `shiftArchive` — legacy `shiftArchive()` (`stbPlayer.js:L6148`) + `_shiftArchive()` (`stbPlayer.js:L6161`); current `shiftArchive()` + `_shiftArchive()` (`src/channels/index.ts:L1805`); fix: when `e === -6e6`, use assignment `_shiftSec = e` (not `_shiftSec += e`) before calling `_shiftArchive()` to match legacy L6150.
- `playArchive` — legacy `stbPlayer.js:L3681` `playArchive(e)` with `updateArchiveInfo(e)`, `showChanelInfo(1)`, `primaryIndex`, `curList`, `epgArray`, `s.time_to`, `fileArchive`, `stbPlay`, `stbSetPosTime`; TS `src/channels/index.ts` now matches: `updateArchiveInfo(e)`, `w.showChanelInfo(1)`, `curList[primaryIndex]`, `epgArray[curProg]`, `prog.time_to`, `fileArchive`, `w.stbPlay`, `w.stbSetPosTime`; function signature `playArchive(e: number): void` identical, all parameter usage and side effects match.
- `channelsList` — legacy `_channelsList()` (`stbPlayer.js:L2702`) full implementation; current stub `channelsList()` (`src/channels/index.ts:L2006`) delegates to `showPage()`, full logic in `_channelsList()` (`src/provider/index.ts:L1654`); callers: `epgKeyHandler`, `bucketsKeyHandler`, `searchChannel`, `channelsKeyHandler` call `window.channelsList`.
- `keyHandler` — legacy `stbPlayer.js:L7088` keyHandler, `keyFun()` (`L7377`), dialog/select/edit mode handlers; TS `src/keyhandler/index.ts:L102` now matches: added `sArrowFun == 1` check in `handleListKey()` for LEFT/RIGHT volume control, added RW/FF/CH_UP/CH_DOWN → page jump in list mode, added PREV/NEXT page navigation with `sPNFun == 3` variant, fixed N8 to map to STOP with showShift, added `liveStop()` for N0 when `playType <= 0` and `nProg` empty; added `keyFun`(20/21) calls for N2/N5 in archive mode; added missing `case keys.PRECH → prevProg()` (legacy L7262) and PLAY/PAUSE in live mode (playType==0) now calls `liveStop()` before falling through to archive pause/play toggle (legacy L7243-7245); STOP now calls `showShift(_(playType ? "Live" : "Restart stream"))` before `playChannel` (legacy L7253-7254).
- `setListDataArray` — missing in legacy (`stbPlayer.js`), present at `src/app/state.ts:109`, caller `src/view/channel-list.ts:23`
- `initPlayer` — `initPlayer` does not exist in either codebase; the startup entry point is `startPlayer` on both sides. Legacy `startPlayer()` at `stbPlayer.js:L8783`; TS `startPlayer()` at `src/index.ts:L1279` (build entry, wins in bundle) and `src/app/init.ts:L76` (duplicate). Fixes in `src/index.ts`: (1) added missing `!== false` guard on `stbInit()` call to match legacy L8796: `if (typeof stbInit === "function" && stbInit() !== false) { onStbReady(); }`; (2) removed extra `window.onkeydown = keyHandler` assignment absent from legacy; (3) added missing IID display line (legacy L8785: `"<br/>IID: " + (__iid ? "..." + __iid.substr(-7) : "-")`); (4) wrapped `benchy_startPlayer` in `setTimeout(..., 23)` per legacy L8786 (was called immediately); (5) removed extra `(window as any).listPodval = (window as any).listPodvalElement` assignment absent from legacy. Same guard fix applied to `src/app/init.ts` for parity.
- `keyHandler` — legacy `stbPlayer.js:L7088` keyHandler, `keyFun()` (`L7377`), dialog/select/edit mode handlers; TS `src/keyhandler/index.ts:L102` now matches: added `sArrowFun == 1` check in `handleListKey()` for LEFT/RIGHT volume control, added RW/FF/CH_UP/CH_DOWN → page jump in list mode, added PREV/NEXT page navigation with `sPNFun == 3` variant, fixed N8 to map to STOP with showShift, added `liveStop()` for N0 when `playType <= 0` and `nProg` empty; added `keyFun`(20/21) calls for N2/N5 in archive mode; added missing `case keys.PRECH → prevProg()` (legacy L7262) and PLAY/PAUSE in live mode (playType==0) now calls `liveStop()` before falling through to archive pause/play toggle (legacy L7243-7245).
- `setListDataArray` — missing in legacy (`stbPlayer.js`), present at `src/app/state.ts:109`, caller `src/view/channel-list.ts:23`
- `initPlayer` — `initPlayer` does not exist in either codebase; the startup entry point is `startPlayer` on both sides. Legacy `startPlayer()` at `stbPlayer.js:L8783`; TS `startPlayer()` at `src/index.ts:L1279` (build entry, wins in bundle) and `src/app/init.ts:L76` (duplicate); fix: added missing `!== false` guard on `stbInit()` call in both TS files to match legacy L8796: `if (typeof stbInit === "function" && stbInit() !== false) { onStbReady(); }`; also removed extra `window.onkeydown = keyHandler` assignment absent from legacy.
- `toggleFullscreen` — no function named `toggleFullscreen` in legacy; equivalent is `stbToFullScreen()` defined at `stb/core.js:L86` (sets `_full = true`, CSS on `#video` + `#vdiv`, calls `_aspect()`) plus companion `stbSetWindow()`. Callers in legacy: `stb/core.js:L328` (after stbInit) and `stbPlayer.js:L1731` (inside `closeList()`). TS has matching `stbToFullScreen()` (`src/core/index.ts:380`) and `stbSetWindow()` (`src/core/index.ts:393`); CSS props and `_aspect`/`applyAspectRatio()` equivalent; flag name differs (`_full` vs `isFullscreen`) — both module-local, not observable from outside. Exposed on window at `src/index.ts:1633` (`window.stbToFullScreen = stbToFullScreen`) and `src/index.ts:1634` (`window.stbSetWindow = stbSetWindow`); `closeList()` (`src/ui/index.ts:783`) calls `window.stbToFullScreen()` like legacy L1731. Browser-API fullscreen helpers (`isNormalScreen`/`openFullscreen`/`closeFullscreen` at `src/core/index.ts:117/137/151`, plus `stbEventToKeyCode` keyCode-76 toggle at L165) are TS-only additions not present in legacy — out of audit scope (not a port, no legacy counterpart to fix). Fix: `stbSetWindow()` was reading module-local `listPos` (hardcoded `0`, never synced from settings) instead of `window.sListPos` which is written by `syncSettings()` at `src/index.ts:3702`. Changed to `(window as any).sListPos ? 758 * w + "px" : 10 * w + "px"` — matches legacy `stbSetWindow()` (`stb/core.js:91`) which uses `sListPos` directly. Status: fixed.
- `toggleFullscreen` — no function named `toggleFullscreen` in legacy; equivalent is `stbToFullScreen()` defined at `stb/core.js:L86` (sets `_full = true`, CSS on `#video` + `#vdiv`, calls `_aspect()`) plus companion `stbSetWindow()`. Callers in legacy: `stb/core.js:L328` (after stbInit) and `stbPlayer.js:L1731` (inside `closeList()`). TS has matching `stbToFullScreen()` (`src/core/index.ts:380`) and `stbSetWindow()` (`src/core/index.ts:393`); CSS props and `_aspect`/`applyAspectRatio()` equivalent; flag name differs (`_full` vs `isFullscreen`) — both module-local, not observable from outside. Exposed on window at `src/index.ts:1633` (`window.stbToFullScreen = stbToFullScreen`) and `src/index.ts:1634` (`window.stbSetWindow = stbSetWindow`); `closeList()` (`src/ui/index.ts:783`) calls `window.stbToFullScreen()` like legacy L1731. Browser-API fullscreen helpers (`isNormalScreen`/`openFullscreen`/`closeFullscreen` at `src/core/index.ts:117/137/151`, plus `stbEventToKeyCode` keyCode-76 toggle at L165) are TS-only additions not present in legacy — out of audit scope (not a port, no legacy counterpart to fix). No mismatch — matches.
- `setListSelectionIndex` — absent from legacy (`stbPlayer.js`); no setter/getter pair for `listSelectionIndex` exists. Legacy mutates `primaryIndex` directly (e.g. `primaryIndex = t` at L1368). TS defines `let listSelectionIndex = 0` (`src/app/state.ts:36`) + setter `setListSelectionIndex(val: number): void` (`src/app/state.ts:106`) + getter `getListSelectionIndex()` (sister function in same file). Caller: `src/view/channel-list.ts:24` (`showChanelsList` uses `catIndex >= 0 ? catIndex : 0` as initial index). Status: TS-only encapsulation wrapper — no legacy counterpart to match. No fix needed.
- `setDetailListActionFn` — legacy `var detailListAction` (stbPlayer.js:L1529, no setter, direct assignment at 20+ sites); TS `setDetailListActionFn(val: (() => void) | null): void` (src/app/state.ts:117) + state `detailListActionFn` (L39). Callers: legacy assigns directly (e.g. L2747 `detailListAction = detailProg`, L2995 `= function(){}`); TS caller `src/view/channel-list.ts:26` invokes `setDetailListActionFn(() => undefined)`. No fix needed — setter pattern matches `setGetListItemFn`/`setListKeyHandlerFn` siblings; legacy semantics (store and retrieve a no-arg callback) preserved.
- `keyHandler` — legacy `stbPlayer.js:L7088` keyHandler, `keyFun()` (`L7377`), dialog/select/edit mode handlers; TS `src/keyhandler/index.ts:L102` now matches: added `sArrowFun == 1` check in `handleListKey()` for LEFT/RIGHT volume control, added RW/FF/CH_UP/CH_DOWN → page jump in list mode, added PREV/NEXT page navigation with `sPNFun == 3` variant, fixed N8 to map to STOP with showShift, added `liveStop()` for N0 when `playType <= 0` and `nProg` empty; added `keyFun`(20/21) calls for N2/N5 in archive mode.
- `playChannel` — legacy `_playChannel(e, t)` (`stbPlayer.js:L1393`); TS `_playChannel(catIdx, chIdx)` (`src/app/playback.ts:L24`). Two mismatches fixed: `showChanelInfo(1)` was `showChanelInfo(settings.infoTimeout)` — reverted to literal `1` to match legacy; removed TS-only `if (typeof setPlayer === "function") setPlayer()` call absent from legacy between `playType = 0` and `stbPlay()`.
- `channelsList` — legacy `_channelsList()` (`stbPlayer.js:L2702`) full implementation; current stub `channelsList()` (`src/channels/index.ts:L2006`) delegates to `showPage()`, full logic in `_channelsList()` (`src/provider/index.ts:L1654`); callers: `epgKeyHandler`, `bucketsKeyHandler`, `searchChannel`, `channelsKeyHandler` call `window.channelsList`.
- `searchRec` — legacy `searchRec()` (`stbPlayer.js:L3045`) is a 50-line dialog opener (sets `window.editCaption`, reads `medSearch` from storage, overrides `window.setEdit` to filter `_crData.data` by name/descr, wires a dedicated listKeyHandler with all key cases and `sArrowFun`/`sRewFun`/`sPNFun` guards, updates #listCaption / #listPodval, hides #listPopUp, calls `window.showPage`, then invokes `window.showEditKey`); current `searchRec(query: string): void` (`src/channels/index.ts:2633`) was a no-op setter that assigned `searchText = query` and was unused. Fix: replaced stub with full legacy port matching the same dialog/listKeyHandler/caption/podval flow.
- `setListDataArray` — missing in legacy (`stbPlayer.js`), present at `src/app/state.ts:109`, caller `src/view/channel-list.ts:23`
- `searchMedia` — legacy `searchMedia(e)` (`stbPlayer.js:L3314`) full edit-UI flow: sets `editCaption = _("String for search")`, reads `medSearch` from storage, sets `editvar`, assigns `setEdit` callback that on submit persists `medSearch`, sets `mediaName = e.title`, unshifts 0 into `mediaSelects`, calls `mediaList(e.playlist_url + ("?" if no `?` else "&") + "search=" + t)`, then calls `showEditKey()`. Current `searchMedia(query: string)` (`src/channels/index.ts:L2624`) was a setter stub writing `searchText = query`; fix: replaced stub with full port matching legacy body exactly — signature `searchMedia(e: any): void`, opens inline editor with `medSearch` storage seed, persists on submit, mutates `window.mediaName`/`window.mediaSelects`, calls `window.mediaList` with search-suffixed URL, invokes `window.showEditKey`. Caller `selectMedia()` (legacy `stbPlayer.js:L3376`) passes the media-item object `e` when `e.search_on` is truthy.
- `getFilteredChannelList` — missing in legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2837`. Legacy `searchChannel()` (`stbPlayer.js:L2307`) sets `searchText` but the channel list UI uses `curList` directly without a separate filtered-getter export. No legacy counterpart to match; function is dead code with no callers outside its own module. Fix: removed `getFilteredChannelList` and its JSDoc from `src/channels/index.ts`.
- `searchHistoryChannel` / `getFilteredHistory` — missing in legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2815` and `:2823`. No legacy counterpart to either function. Both are dead code: `searchHistoryChannel` has no callers outside its own module; `getFilteredHistory` is only consumed by its sibling `searchHistoryChannel`. State `historySearchText` (`src/channels/index.ts:219`) is read by neither after this removal. Fix: removed `searchHistoryChannel`, `getFilteredHistory`, and the orphan `historySearchText` from `src/channels/index.ts`. Supersedes the PR #149 "no fix" decision for `getFilteredHistory` — both are TS-only speculative additions with no legacy basis.
- `searchHistoryChannel` / `getFilteredHistory` — both absent from legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2815` and `:2823`. No legacy counterpart. Both are dead code: `searchHistoryChannel` has no callers outside its own module; `getFilteredHistory` was only consumed by `searchHistoryChannel`. State `historySearchText` (`src/channels/index.ts:219`) is orphaned after removal. Fix: removed both functions plus the orphan `historySearchText`. Supersedes the PR #149 "no fix" decision for `getFilteredHistory`.
- `searchRec` — adjacent observation (out-of-session): current `searchRec(query: string)` (`src/channels/index.ts:L2633`) is same stub class as `searchMedia` was — writes `searchText = query`. Legacy `searchRec()` (`stbPlayer.js:L3045`) is full edit-UI flow: filters `_crData.data` by name/descr, installs custom `listKeyHandler` (EXIT/LEFT/RETURN/RIGHT/N2/INFO/RW/PREV/FF/NEXT/N0/YELLOW/TOOLS/ENTER), sets `getListItem`/`detailListAction`/`listCaption`/`listPodval`, calls `showPage()`. TS port also lacks `infoProgramm`/`catRecordsList`/`_crData` definitions. Deferred to its own audit session per Phase 0.4 one-function rule; flagged here for tracking.
- `getVolume` — not defined as a local function in either legacy (`stbPlayer.js`) or current TS. Both call the platform-injected `stbGetVolume` (external STB API, not defined in either codebase). Legacy: 7 refs to `stbGetVolume`, TS: 17 refs. TS `changeVolume(delta)` (`src/ui/index.ts:L2129`) correctly guards with `typeof (window as any).stbGetVolume !== "function"` before calling it — matches legacy `changeVolume()` (`stbPlayer.js:L5973`). No local `getVolume` wrapper exists to port.
- `setVolume` — `setVolume(level?, step?)` (`src/commands/index.ts:L286`) is TS-only. Legacy (`stbPlayer.js`) has no `setVolume` — the only volume function is `changeVolume(e)` (`stbPlayer.js:L5973`) which adds `e` to current volume, clamped [0,100]. TS already has a matching `changeVolume(delta: number)` (`src/ui/index.ts:L2129`) that mirrors legacy exactly (stbGetVolume + delta, clamp, stbSetVolume, _showVolume). The webhook command dispatcher (`src/commands/index.ts:L382`) calls `setVolume(cmd.volume, cmd.volume_step)` for absolute/relative remote control — this dual-mode wrapper is TS-only and absent from legacy. No fix needed — TS-only convenience wrapper with no legacy counterpart to match.
- `setChannelList` — absent from both legacy (`stbPlayer.js`, case-insensitive grep) and current TS (`src/**/*.ts`). Neither codebase defines or invokes a `setChannelList` function. Channel list is mutated via direct assignment to global `curList` (legacy: `curList = []` at L748/L8484, `curList = cats[catsArray[catIndex]] || []` at L1367; TS: `(window as any).curList` assignments, e.g. `src/ui/index.ts:L1014`). No TS stub, no legacy counterpart. Status: missing in both → no fix needed; queue entry carried over from initial list as a no-op audit.
- `getFilteredChannelList` — missing in legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2837`. Legacy `searchChannel()` (`stbPlayer.js:L2307`) sets `searchText` but the channel list UI uses `curList` directly without a separate filtered-getter export. No legacy counterpart to match; function is dead code with no callers outside its own module. Fix: removed `getFilteredChannelList` and its JSDoc from `src/channels/index.ts`.
- `searchHistoryChannel` / `getFilteredHistory` — missing in legacy (`stbPlayer.js`); present in TS at `src/channels/index.ts:2815` and `:2823`. No legacy counterpart to either function. Both are dead code: `searchHistoryChannel` has no callers outside its own module; `getFilteredHistory` is only consumed by its sibling `searchHistoryChannel`. State `historySearchText` (`src/channels/index.ts:219`) is read by neither after this removal. Fix: removed `searchHistoryChannel`, `getFilteredHistory`, and the orphan `historySearchText` from `src/channels/index.ts`. Supersedes the PR #149 "no fix" decision for `getFilteredHistory` — both are TS-only speculative additions with no legacy basis.
